//write a cpp program to implement bouncing ball using sine-wave form. Apply the concept of polymorphism

#include <iostream>
#include <graphics.h>
#include <conio.h>
#include <dos.h>

int main(){
    int gd=0,gm,x=20,flag=0,y=200,uplimit=250;
    initgraph(&gd,&gm,"C\\Tc\\BGI");
    while(!kbhit()){
        //what kbhit does is that it iterates the statement until you press a key
        //later getch returns the key stroke
        
    }
}